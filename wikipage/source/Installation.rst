Installation
==================================

FLUXOS can be built in two ways: **inside a container** (recommended for
newcomers and HPC) or **natively** on your host. The container path ships all
system dependencies pre-installed; the native path documents the dependency
matrix and per-OS install steps.

.. toctree::
   :maxdepth: 3

   Download
   Containers
   Build
   Compile
   Trouble
